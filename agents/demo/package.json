{
  "name": "demo-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Project marker for eve's named-agent discovery — without it the agent root resolves to the repo root. Carries its own eve dependency since the nested-web layout removed the repo-root package.json the service build previously borrowed the eve binary from.",
  "dependencies": {
    "ai": "^7.0.58",
    "eve": "0.40.0",
    "zod": "^4"
  }
}
